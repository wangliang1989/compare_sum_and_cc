name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a multi-line script
      run: 
        sudo apt install gfortran
        make
        echo 'time ./sum'
        time ./sum
        echo 'time .cor'
        time ./cor
